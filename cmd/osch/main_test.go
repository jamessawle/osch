package main

// All command behaviour is exercised in cmd/osch/cmd; this file remains as a
// package-level placeholder so the test toolchain has a target for the main
// package itself if one is ever needed.
